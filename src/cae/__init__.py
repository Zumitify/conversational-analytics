"""Conversational Analytics Engine (CAE).

A natural-language interface to analytical databases:
semantic layer -> intent parsing (LLM) -> deterministic planning ->
SQL generation -> validation -> execution -> chart + summary.
"""

__version__ = "0.1.0"
